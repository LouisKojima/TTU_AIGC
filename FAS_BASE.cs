using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace FAS_BASE
{
    public enum StreetObject { None, Fussgaenger, Zweirad, Motorrad, PKW, LKW, Unbekannt };
    public enum PathSide { Left, Mid, Right };
}
